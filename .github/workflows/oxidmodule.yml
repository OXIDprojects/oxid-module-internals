name: oxid module tests

on: [push]

jobs:

  build:
    strategy:
      matrix:
        tag: [v2_6.1_php7.1, v2_6.2-rc_php7.1, v2_dev-master_php7.2]
    services:
      mysql:
        image: mysql:5.7
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: oxid
        ports:
          - 3306
        options: --health-cmd "mysqladmin ping" --health-interval 10s --health-timeout 5s --health-retries 10

    runs-on: ubuntu-latest
    container: oxidprojects/oxid-test:${{matrix.tag}}
    env:
      MODULE_NAME: moduleinternals
    steps:
    - uses: actions/checkout@v1

    - name: Validate composer.json and composer.lock
      run: composer validate

    - name: setup oxid
      run: bash /var/www/oxid/setup.sh

    - name: phpcs
      run: |
        MD=$(pwd)
        cd /var/www/oxid/
        php phpcs.phar --standard=PSR12 --extensions=php --ignore=vendor $MD

    - name: runt tests
      run: |
        cd /var/www/oxid/
        vendor/bin/runtests | ack --passthrough "ok"
